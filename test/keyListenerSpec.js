define(["src/keyListener"], function(keyListener) {
  describe("keyListener module definition", function() {
    it("is defined", function() {
      expect(keyListener).toBeDefined();
    });
  });
});